# The server the client talks to.
[server]
host = localhost
port = 8080
timeout = 2.5
tls = yes

[limits]
max_connections = 512
rate_per_second = 0.5

[paths]
cache = /var/cache/app
log   = /var/log/app.log
